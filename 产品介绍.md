# Meeting Transcriber — 纯离线双轨会议转写工具

> 一款**纯离线**的双轨会议转写桌面应用：同时录制麦克风与系统音频，停录后自动识别说话人角色并转写为分角色 Markdown 会议纪要。**全程脱网，数据不出本机。**

---

## 一、产品定位

### 一句话描述

同时录制**本端麦克风**与**系统播放音（对方声音）**，停录后由本地 AI 完成「说话人分离 + 语音转写」，自动生成标注了发言人姓名的 Markdown 会议纪要。

### 为什么需要它

| 场景 | 痛点 | 本产品方案 |
| :--- | :--- | :--- |
| 线上会议 | 会议软件自带纪要依赖云端、收费 | 本地转写，一次录音永久留存 |
| 敏感会议 | 数据不可上传至云端 | 全程离线，抓包验证零网络请求 |
| 多人会议 | 分不清谁说了什么 | 声纹聚类 + 声纹库自动标注发言人 |
| 资料整理 | 录音文件难检索 | 直接产出分角色文字纪要（MD） |

---

## 二、核心设计原则

| 编号 | 原则 | 含义 |
| :--: | :--- | :--- |
| P1 | **离线是隐私契约** | GUI 运行时严禁发起任何网络请求，模型随包内置，数据不出本机 |
| P2 | **录音零开销** | 录音期间只做采集 + 波形显示，不做 AI 推理；全部分析在停录后离线执行 |
| P3 | **录音与转写解耦** | 模型未加载完成也能先录音，停录后模型就绪自动续转 |
| P4 | **数据安全** | 声纹库、配置文件采用原子写，避免写盘中断导致数据损坏 |

---

## 三、功能特性

### 3.1 双轨录音（A-1 ~ A-7）

- 麦克风 + 系统音**双轨并行采集**，Windows 系统音走 **WASAPI Loopback** 原生回环
- 任意设备采样率/声道数自动重采样为 **16kHz 单声道**
- 系统音默认 0.9 衰减混音，避免盖过本端人声
- 支持**麦克风增益调节**（1–50 倍），应对系统音频软件压制第三方应用麦克风信号的情况
- 超长会议防内存溢出：单轨缓存超限自动 spill 到临时文件

### 3.2 语音识别 ASR（B-1 ~ B-7）

- 内置 **SenseVoice** 离线模型（sherpa-onnx 推理），中文为主，兼英文/日文/韩文/粤语
- **识别语言可配置**：自动 / 中文 / 英文 / 日文 / 韩文 / 粤语
- 分段推理：长音频先断句再逐段送入 ASR，静音段自动跳过省算力
- ASR 输出自动清洗：剥离语言/情绪等特殊标记，只保留纯文本
- 推理线程数可配（1–8，系统推荐 CPU 核数的一半，上限 4）

### 3.3 声纹分角色（C-1 ~ C-8）

- 系统音轨经 **Pyannote 说话人分割**自动聚类为多个 Speaker 区间
- 每个 Speaker 提取 **512 维声纹特征**（3D-Speaker eres2net）
- 本场声纹与**本地声纹库**做余弦相似度匹配（阈值 0.65），命中自动标注真实姓名
- 转写完成后可对未知发言人**命名并注册声纹**，下次会议自动识别
- 声纹库带版本号、损坏自动备份容错

### 3.4 语音活动检测 VAD（D-1 ~ D-4）

- 麦克风轨基于 RMS 能量的轻量 VAD 断句
- 段长约束：最短 1.5s、最长 15s，同角色相邻短段自动合并
- 声纹聚类长段二次切分，防止单段过长导致识别退化

### 3.5 报告输出（E-1 ~ E-7）

- 双轨转写结果按时间戳排序合并，输出**分角色 Markdown 报告**：

```text
# 会议转写报告
生成日期: 2026-08-06

[00:12] 我 (张三) 今天先同步一下进度
[00:18] 发言人1 那部分我来负责
[00:35] 我 (张三) 好，下周复盘
```

- 麦克风轨固定标「我 (姓名)」；系统音轨未注册时标「发言人N」，注册后标真实姓名
- **名词纠错**：支持「错词=正确词」映射表，转写完成后自动全文替换（如专有名词、同音字）
- 报告原子写（先写临时文件再 rename），转写可取消，进度实时显示

### 3.6 导入已有音频（F-1 ~ F-4）

- 支持导入 **WAV / M4A / MP3 / AAC / FLAC** 常见格式
- 任意采样率/编码自动解码，统一走声纹聚类分角色转写
- 模型未就绪时任务挂起，就绪后自动续转

### 3.7 桌面 GUI（G-1 ~ G-14）

- 首次启动强制输入使用者姓名
- 四状态主界面：就绪 / 录音 / 转写 / 完成
- **双轨实时滚动波形**：麦克风采青、系统音粉，轨名标签 + 时间刻度线
- 录音自动缩为**右上角置顶小窗**（停止键 + 波形 + 计时），停录恢复完整窗口
- 转写态：百分比进度条 + spinner + 取消按钮
- 完成态：文本预览 + 「打开文件」+ 发言人面板（命名/改名/注册声纹）
- **设置面板**（四 Tab）：常规（姓名/识别语言/输出目录/线程数）、音频（麦克风选择/系统音开关/增益）、高级纠错（名词映射）、声纹管理（查看/删除）
- 模型状态指示（就绪/加载中/错误+重试）；界面自动跟随系统语言（中/英/日）

### 3.8 命令行模式（H-1 ~ H-3）

```bash
meeting-transcriber --list-devices                          # 枚举音频输入设备
meeting-transcriber --offline input.wav --name 张三 --lang zh --threads 4
meeting-transcriber --help
```

---

## 四、技术架构

### 模型栈

| 组件 | 用途 | 说明 |
| :--- | :--- | :--- |
| SenseVoice-Small | 语音识别 ASR | FunAudioLLM 开源，sherpa-onnx 加载 ONNX 格式，纯本地推理 |
| Pyannote | 说话人分割 | 系统音轨自动聚类为多 Speaker |
| 3D-Speaker (eres2net) | 声纹提取 | 512 维声纹特征 + 余弦匹配 |

### 转写管线

```text
麦克风轨 ──► 能量 VAD 断句 ──┐
                              ├──► 短段合并 / 长段切分 ──► SenseVoice 逐段推理 ──► 名词纠错 ──► 时间轴排序 ──► Markdown 报告
系统音轨 ──► 声纹聚类分割 ────┘            │
              └──► 512维声纹 ──► 余弦匹配本地声纹库（自动标名）
```

### 技术栈

- **Python 3.12** + **PySide6**（Qt 桌面 GUI）
- **sherpa-onnx**（离线推理）、**sounddevice / PyAudioWPatch**（WASAPI loopback 采集）
- **PyInstaller** 打包为免安装分发包

---

## 五、数据与隐私

| 数据 | 位置 | 说明 |
| :--- | :--- | :--- |
| 模型缓存 | `~/.meeting-transcriber/models/` | 打包时内置；运行优先用 exe 同级 `./models/` |
| 用户配置 | `~/.meeting-transcriber/config.json` | 持久化全部设置，原子写 |
| 声纹数据库 | `~/.meeting-transcriber/speakers.json` | 原子写、带版本号、损坏容错 |
| 录音文件 | `输出目录/record_YYYYMMDD_HHMMSS.wav` | 16kHz 单声道 |
| 转写报告 | `输出目录/transcript_YYYYMMDD_HHMMSS.md` | 分角色 Markdown |

**隐私承诺**：GUI 运行全程零网络请求（可抓包验证）；声纹、录音、转写文本全部留在本机；仓库通过 `.gitignore` 排除敏感数据与模型文件。

---

## 六、安装与使用

### 下载运行（推荐）

直接使用打包产物：

```
dist\meeting-transcriber\meeting-transcriber.exe     # 免安装，双击即用（模型已内置）
dist\meeting-transcriber-0.1.0-win64.zip             # 分发压缩包，解压后同上
```

### 从源码开发运行

```bash
pip install -e ".[dev]"                  # 安装运行时依赖 + pytest
python scripts/download_models.py        # 下载三个模型（SenseVoice / eres2net / Pyannote）
python -m meeting_transcriber.gui.app    # 启动 GUI
python -m pytest tests/ -q               # 运行测试（150 项）
```

### 典型使用流程

1. 双击启动 exe，首次输入姓名
2. 点「开始录音」→ 缩为右上角小窗，说话即见双轨滚动波形
3. 点「停止录音」→ 自动进入离线转写（进度条 + 可取消）
4. 完成后预览报告，为未知发言人命名/注册声纹
5. 「打开文件」定位生成的 Markdown 纪要

### Windows 打包

```powershell
powershell -ExecutionPolicy Bypass -File scripts/build_windows.ps1
```

---

## 七、系统要求

- **Windows 10/11**（首要交付平台；架构预留 macOS / Linux 扩展）
- 64 位系统，8GB 内存以上推荐
- 扬声器/麦克风可用的音频设备
- 离线使用，无需联网

---

## 八、已知边界（诚实说明）

- ASR 准确率受环境噪音、口音、语速影响；可通过**设置 → 高级纠错**配置错词映射来兜底，或切换**识别语言**提升特定场景效果
- 声纹识别依赖说话人音色区分度；两人音色极接近时可能出现误判，建议注册声纹后反复验证
- 模型为 int8 量化版本，在保证速度的同时精度略低于全精度模型
