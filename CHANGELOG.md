# Changelog

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-08-06

### 新增（初始发布）

- 双轨录音：麦克风 + 系统音频（WASAPI Loopback）并行采集，自动重采样 16kHz 单声道
- 实时双轨滚动波形（麦克风采青 / 系统音粉），带轨名标签与时间刻度线
- 录音小窗模式：录音时缩为右上角置顶小窗（停止键 + 波形 + 计时）
- SenseVoice 离线语音识别（sherpa-onnx 推理，中/英/日/韩/粤语，可指定识别语言）
- Pyannote 说话人分割 + 3D-Speaker 512 维声纹提取 + 本地声纹库余弦匹配（阈值 0.65）
- 能量 VAD 断句 + 短段合并 + 长段二次切分
- 分角色 Markdown 会议报告（麦克风轨标「我 (姓名)」，系统音轨标发言人）
- 名词纠错映射表（「错词=正确词」自动全文替换）
- 导入外部音频转写（WAV / M4A / MP3 / AAC / FLAC）
- 桌面 GUI：四状态机 + 设置面板（常规/音频/纠错/声纹管理）+ 模型状态指示
- 命令行模式：`--offline` / `--list-devices` / `--help`
- Windows 打包脚本（PyInstaller onedir，模型内置）
- 模型下载脚本（SenseVoice / eres2net / Pyannote，含代理与镜像回退）
- 150 项自动化测试

### 修复

#### 崩溃类

- 修复「开始录音」时 Qt6Core.dll 0xc0000409 崩溃（BEX64）：
  QThread 在运行中被提前析构 → 引用释放推迟到 `finished` 信号后触发
- 新增崩溃取证：Python/C++ 异常与 Qt 致命消息写入 `gui_error.log`，
  打包版 exe 无控制台时仍可定位崩溃
- 修复采集线程异常被静默吞掉：失败信息上抛提示用户，另一轨降级产出
- 修复配置文件 / 声纹库损坏导致应用无法启动：回退默认值 / 备份后以空库继续

#### 录音采集

- 修复「系统音」轨实际采集到麦克风信号：改用 PyAudioWPatch WASAPI loopback 真回采
- 修复 `Multiple input devices found`：同名设备多 hostapi（MME/DirectSound/WASAPI）消歧优先 MME
- 修复麦克风信号弱 / 无波形（系统音频软件压制）：新增增益调节（默认 10x，1–50 可调）
- 修复 48kHz 立体声折叠错误：正确重采样为 16kHz 单声道
- 修复长会议内存溢出：单轨 PCM 缓存超限自动 spill 到临时文件

#### 转写管线

- 修复转写报告全空：VAD 最短段长由 4s 放宽至 1.5s，不再丢弃真实短句
- 修复 ASR 输出夹带 `<|zh|>` 等特殊标记：输出清洗只保留纯文本
- 修复声纹库未注入转写管线：工作线程显式传递 speaker_db，注册过的人自动标名
- 修复报告预览 / 首次生成 / 改名重写格式不一致：收敛为单一真相源
- 修复写盘中断残留半截文件：原子写（先写临时文件再 rename）

#### GUI / 交互

- 修复设置入口缺失：主窗口顶部栏新增「设置」按钮
- 修复进度条冲到 150%：双轨进度 span 修正 + 进度钳制 [0, 100%]
- 修复识别语言设置不生效：语言变更后自动重载 ASR 模型
- 修复取消转写后残留半截报告：写盘前检查取消状态

#### 安全 / 健壮性

- 修复模型下载 tar 解包路径穿越：过滤 `../` 路径
- 修复模型下载不幂等：`.part` 临时文件 + Content-Length 校验

### 已确认非缺陷

- 打包脚本退出码 1：沙箱拦截 PyInstaller 清理 Python 缓存所致，非打包失败，产物正常
